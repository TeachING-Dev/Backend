spring:
  config:
    activate:
      on-profile: prod

qdrant:
  url: ${QDRANT_URL}
  api-key: ${QDRANT_API_KEY}
  collection-name: ${QDRANT_COLLECTION_NAME}
  vector-size: ${QDRANT_VECTOR_SIZE:1536}
